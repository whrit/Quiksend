/**
 * Placeholder. The TanStack Start app — file-based routes, `createServerFn` RPC,
 * the Better Auth handler mount (`/api/auth/$`), the shadcn/ui + Tailwind app shell,
 * and the workspace switcher — is scaffolded in Phase 1.
 *
 * This module keeps `apps/web` a valid workspace package so the monorepo structure
 * and the `pnpm check` gate are complete from day one.
 */
export const PLACEHOLDER = "phase-1" as const;
